name: Deploy Orchestration API
on:
  push:
    paths:
      - 'orchestration-api/**'
      - 'ml/**'
      - 'docker-compose.yml'
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "Deploy orchestration-api"
